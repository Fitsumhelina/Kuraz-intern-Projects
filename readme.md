# 🗂️ Simple Task Manager API

## 📌 Endpoints

- `GET /api/tasks` - List all tasks
- `POST /api/tasks` - Add new task `{ "title": "Task Title" }`
- `PUT /api/tasks/:id` - Mark task as completed
- `DELETE /api/tasks/:id` - Delete a task

## ▶️ Run the Project

```bash
npm install
node index.js
